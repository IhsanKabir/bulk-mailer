openpyxl==3.1.5
requests==2.33.1
keyring==25.7.0
sv-ttk==2.6.1
pywin32==312
msal==1.37.0
pyinstaller==6.20.0
# WhatsApp Blast drives the user's installed Chrome/Edge via Playwright.
# We do NOT run `playwright install` — the app uses the system browser
# (channel="chrome"/"msedge"), so no ~150 MB Chromium is bundled; only
# Playwright's own driver ships in the exe.
playwright==1.49.1
